{
  "name": "cht-websheet",
  "version": "0.1.0",
  "license": "GPL-3.0-only",
  "type": "module",
  "scripts": {
    "predev": "npm run build:app && npm run install:runtime",
    "dev": "vite --host 127.0.0.1",
    "build:app": "vite build",
    "build": "npm run build:app && npm run install:runtime",
    "preview": "vite preview --host 127.0.0.1",
    "install:runtime": "node scripts/install-standalone-runtime.mjs",
    "build:packages": "node scripts/build-runtime-packages.mjs",
    "pretest:e2e": "npm run build",
    "test:e2e": "node scripts/e2e-real-upload.mjs"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "hyperformula": "^3.2.0",
    "jquery": "^3.7.1",
    "jszip": "^3.10.1",
    "lucide": "^0.468.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "vite": "^8.0.13"
  }
}
